import React, { useContext } from 'react'
import { Col } from 'react-bootstrap'

const DeviceItem = ({currDevice}) => {    
    return (
        <Col md='3'>
            item
        </Col>
    )
}

export default DeviceItem